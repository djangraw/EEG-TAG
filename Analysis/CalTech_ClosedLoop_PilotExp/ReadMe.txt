The XML files left in this directory are copies of the final versions
of each XML, ie these are the XMLs that have had the EEG confidence 
values written in for each image.

The XML file produces by the matlab code is not stored here, but
rather moved directly to the directory on the presentation laptop
where they are used to control the images shown during the next
iteration.